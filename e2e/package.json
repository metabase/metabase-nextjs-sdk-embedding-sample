{
  "name": "e2e",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "@testing-library/cypress": "^10.0.3",
    "cypress": "^14.0.3",
    "dotenv-cli": "^8.0.0"
  },
  "scripts": {
    "cypress:open": "dotenv -e ../.env.docker -- npx cypress open --config-file ./support/cypress.config.js --e2e",
    "cypress:run": "dotenv -e ../.env.docker -- npx cypress run --config-file ./support/cypress.config.js"
  }
}
